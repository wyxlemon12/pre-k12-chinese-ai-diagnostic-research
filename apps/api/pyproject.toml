[project]
name = "pre-k12-api"
version = "0.1.0"
description = "FastAPI harness for the Pre-K12 Chinese AI diagnostic assistant."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "fastapi[standard]>=0.116.0",
  "pydantic-settings>=2.10.0",
]

[tool.uv]
package = false
